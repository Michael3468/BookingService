mixin toggle(params = {})
  -
    const {
      leftCaption = '',
      toggleBtnText = 'Получать спецпредложения',
      isChecked = '',
      toggleName = 'specialOffers',
      toggleId = 'toggle',
    } = params

  .toggle
    if (leftCaption)
      h3.toggle__caption= leftCaption
    .toggle__body
      if (isChecked === 'true')
        input.toggle__button.js-toggle__button(
          id = toggleId,
          type = "checkbox"
          checked = isChecked
          name = toggleName
        )
      else
        input.toggle__button.js-toggle__button(
          id = toggleId,
          type = "checkbox"
          name = toggleName
        )

      label.toggle__text(for = toggleId) #{toggleBtnText}
