mixin dropdown-guests(params)
  h3.dropdown-guests__caption= params.leftCaption
  .dropdown-guests__block(id=`dropdown-guests${params.idPostfix}` class=params.menuStatus)
    .dropdown-guests
      p.dropdown-guests__selection_text= params.selectionText
      .dropdown-guests__expand-button.material-icons expand_more
      
    .dropdown-guests__menu(class=params.expMenuStatus)

      if (params.idPostfix == '-changed-example')
        - var items = { 'item1': {valName : 'Взрослые', valCounter: '2', valClass: 'dropdown-guests__button-decrement_dark'},'item2': {valName : 'Дети', valCounter: '1', valClass: 'dropdown-guests__button-decrement_dark'},'item3': {valName : 'Младенцы'} };
      else
        - var items = { 'item1': {valName : 'Взрослые'},'item2': {valName : 'Дети'},'item3': {valName : 'Младенцы'} };

      each val, key in items
        .dropdown-guests__menu_option(id=key)
          p.dropdown-guests__menu-item= val.valName
          .dropdown-guests__menu-item-controls
            button.dropdown-guests__button-decrement(id=`dropdown-guests${params.idPostfix}-${key}-dropdown-guests__button-decrement` class=(val.valClass))
              i.dropdown-guests__button-decrement_icon
            span.dropdown-guests__counter(id=`dropdown-guests${params.idPostfix}-${key}-counter`)= val.valCounter || 0
            button.dropdown-guests__button-increment(id=`dropdown-guests${params.idPostfix}-${key}-dropdown-guests__button-increment`)
              i.dropdown-guests__button-increment_icon
      
      .dropdown-guests__buttons
        if (params.cleanButtonStatus == 'show')
          - var cleanButtonClass = 'dropdown-guests__button_clean_'+params.cleanButtonStatus

        button.dropdown-guests__button_clean(id=`dropdown-guests${params.idPostfix}-button-clean` class=cleanButtonClass) очистить
        button.dropdown-guests__button_apply(id=`dropdown-guests${params.idPostfix}-button-apply`) применить

//- examples

//- +dropdown-guests({leftCaption:    'dropdown',
//-                   selectionText:  'Сколько гостей',
//-                   idPostfix:      '-2',
//-                   items:          ['Взрослые','Дети','Младенцы'],
//-                   menuStatus:     'menu-open',
//-                  })

//- +dropdown-guests({leftCaption:    'dropdown',
//-                   selectionText:  '3 гостя',
//-                   idPostfix:      '-changed-example',
//-                   items:          ['Взрослые','Дети','Младенцы'],
//-                   menuStatus:     'menu-open',
//-                   cleanButtonStatus: 'show'
//-                  })

//- +dropdown-guests({leftCaption:   'dropdown',
//-                   idPostfix:     '-1',
//-                   selectionText: 'Сколько гостей',
//-                   expMenuStatus: 'visibility-hidden',
//-                  })