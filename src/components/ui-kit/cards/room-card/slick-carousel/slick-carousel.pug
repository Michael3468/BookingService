mixin slick-carousel(params = {})
  -
    const {
      photoName = ''
    } = params

  .slick-picture.js-slick-picture
    img.slick-item_picture(
      src = `./assets/img/${photoName}`,
      loading = "lazy",
      alt = "фото номера",
    )

    img.slick-item_picture(
      src = `./assets/img/${photoName}`,
      loading = "lazy",
      alt = "фото номера",
    )

    img.slick-item_picture(
      src = `./assets/img/${photoName}`,
      loading = "lazy",
      alt = "фото номера",
    )

    img.slick-item-picture(
      src = `./assets/img/${photoName}`,
      loading = "lazy",
      alt = "фото номера",
    )
